{
  "name": "qaq",
  "version": "0.1.0",
  "description": "DeepSeek Harness launch resilience guard: detect host/UI boot failure, roll back to last-good profile config, and restart. Non-invasive; runs as a visible CMD window.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "author": "WTStarMark",
  "engines": {
    "node": ">=22.19.0"
  },
  "packageManager": "pnpm@11.7.0",
  "bin": {
    "qaq": "./bin/qaq.mjs"
  },
  "scripts": {
    "build": "esbuild src/cli.ts --bundle --platform=node --format=esm --target=node22 --banner:js=\"import { createRequire as __qaqCRequire } from 'module'; const require = __qaqCRequire(import.meta.url);\" --outfile=dist/qaq.mjs",
    "qaq": "tsx src/cli.ts",
    "test": "vitest run",
    "smoke": "node tools/smoke.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.10",
    "esbuild": "^0.28.0",
    "tsx": "^4.22.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.0"
  }
}
